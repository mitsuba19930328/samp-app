
<h1>タスク一覧</h1>

<%=link_to '新規登録', new_task_path, class:'btn btn-primary' %>

<div class="mb-3">
  <table class="table table-hover">
    <thead class="thead-default">
      <tr>
        <th><%= Task.human_attribute_name(:name) %></th>
        <th><%= Task.human_attribute_name(:created_at) %></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @tasks.each do |task| %>
        <tr>
          <td><%= link_to "#{task.name}", task %></td>
          <td><%= task.created_at %></td>
          <td><%= link_to "編集", edit_task_path(task), class: 'btn btn-primary mr-3' %></td>
          <td><%= link_to "削除", task, method: :delete, data: {confirm: "タスク#{task.name}を削除します。よろしいですか？"}, class: 'btn btn-danger' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<!--<div class="form">-->
<!--  <p><textarea id='inputBox' style='width: 800px;'><%#=@text %></textarea></p>-->
<!--  <p>変更したい文字：<input type='text' id='beforeText'></p>-->
<!--  <p>変更する文字：<input type='text' id='afterText'></p>-->
<!--  <button id='changeButton'>変換！</button>-->
<!--</div>-->
<!--<div>-->
<!--今回の練習用アプリでは、、Javascriptをrailsに組み込むことを学習の第一目標とする。-->
<!--ある程度の動きが作れるようになったら、別アプリ（ポートフォリオ用アプリ）を作成-->
<!--awsの構築を本を読みながら並行作業で行っていく。（読みながら手を動かす）-->
<!--その後、dockerに関しても同様に行っていく。-->
<!--</div>-->